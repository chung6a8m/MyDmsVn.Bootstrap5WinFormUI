namespace MyDmsVn.Bootstrap5WinFormUI.Controls;

/// <summary>Specifies the shared visual size of children in an input group.</summary>
public enum BootstrapInputGroupSize
{
    /// <summary>Uses the small theme metrics.</summary>
    Small,
    /// <summary>Uses the default theme metrics.</summary>
    Default,
    /// <summary>Uses the large theme metrics.</summary>
    Large
}
