# v1 Public API Baseline

## Baseline decision

Phase 16 froze the Phase 15-reviewed exported API as the proposed v1 compatibility baseline beginning with `1.0.0-rc.1`.

`BootstrapPagination`, Stages 1–9 component additions, managed Tooltip placement, the shared overlay enums, and `BootstrapPopover` are intentional compatible additions made while the package remains on the release-candidate line. Each API addition was reviewed through the existing fingerprint gate before the approved baseline was updated.

The baseline covers every exported type plus each declared public, protected, and protected-internal constructor, field, property, event, and method in the core assembly. Including protected surface is intentional because subclasses can depend on it.

Approved SHA-256 API fingerprint:

```text
83574b5a78bd563ccbe641845b1a1a0c9e2eee1e0f717fba0ecf25cf89f35ba8
```

The reviewed Pagination addition exports only `MyDmsVn.Bootstrap5WinFormUI.Controls.BootstrapPagination`. Its declared surface is the parameterless constructor, `TotalItems`, `PageSize`, `CurrentPage`, `TotalPages`, `MaxVisiblePages`, `ShowFirstLast`, `ShowPreviousNext`, `ButtonSize`, `Variant`, `BorderRadius`, `PageChanged`, and the `GetPreferredSize(Size)` override inherited as part of the control contract. Pagination layout helper types remain internal.

The reviewed Badge addition exports only `MyDmsVn.Bootstrap5WinFormUI.Controls.BootstrapBadge`. Its declared surface is the parameterless constructor, `Variant`, `CustomColor`, `Pill`, `BorderRadius`, `GetPreferredSize(Size)`, and the protected lifecycle/painting overrides required by the custom `Control` implementation (`Dispose`, `OnAutoSizeChanged`, `OnDpiChangedAfterParent`, `OnEnabledChanged`, `OnFontChanged`, `OnPaint`, and `OnTextChanged`). `BootstrapBadgeRenderLogic` and `BootstrapBadgePalette` remain internal; no new public enum, timer, theme service, geometry type, or dependency was introduced.

The reviewed Alert addition exports only `MyDmsVn.Bootstrap5WinFormUI.Controls.BootstrapAlert`. Its declared surface is the parameterless constructor, `Variant`, `Icon`, `IconRenderer`, `Dismissible`, `BorderRadius`, `Dismissed`, `Dismiss()`, and the protected lifecycle/painting overrides required by the `UserControl` implementation (`Dispose`, `OnDpiChangedAfterParent`, `OnEnabledChanged`, `OnFontChanged`, `OnLayout`, `OnPaint`, and `OnTextChanged`). `BootstrapAlertRenderLogic`, `BootstrapAlertPalette`, `BootstrapAlertMetrics`, and `BootstrapAlertLayout` remain internal. No new public enum, timer, animation scheduler, theme service, geometry type, overlay/popup abstraction, or dependency was introduced.

The reviewed overlay addition exports `BootstrapOverlayPlacement` (Auto plus Top/Bottom/Left/Right center/Start/End), `BootstrapOverlayCollisionBehavior` (None, Flip, Shift, FlipAndShift), `BootstrapTooltipPositioning` (Native, Managed), `BootstrapPopoverTrigger` (Click, Manual), and `BootstrapPopover : Component`. Tooltip adds only `Positioning`, `Placement`, `CollisionBehavior`, `Offset`, and `BoundaryPadding` to its prior surface; it still exposes no public Show/Hide/Content or native ToolTip. Popover declares its parameterless and `IContainer` constructors; `Target`, `Content : Control`, trigger/placement/collision/spacing/chrome/close properties, read-only `IsOpen`, `Opened`/`Closed`, `Show()`/`Hide()`/`Toggle()`, and protected `Dispose(bool)`. Placement request/result/overflow, engine, surface, dropdown, renderer, and anchor tracker remain internal. No custom Form, global hook, timer, second theme/DPI system, rich-content parser, or package dependency was introduced.

The reviewed Tabs addition exports `MyDmsVn.Bootstrap5WinFormUI.Controls.BootstrapTabStyle` with exactly `Tabs = 0`, `Pills = 1`, and `Underline = 2`, plus `MyDmsVn.Bootstrap5WinFormUI.Controls.BootstrapTabControl : System.Windows.Forms.TabControl`. The control's declared surface is the parameterless constructor, `TabStyle`, `Variant`, `Fill`, `BorderRadius`, and the protected `Dispose(bool)` lifecycle override. `TabPages`, `SelectedIndex`, `SelectedTab`, `SelectedIndexChanged`, keyboard/focus behavior, images/tooltips, and overflow remain inherited native WinForms surface rather than being re-declared. `BootstrapTabControlRenderLogic`, its palette/metrics/layout structs, and all hover/layout/theme-font implementation details remain internal/private. No timer, animation scheduler, custom page/window host, P/Invoke, icon model, geometry type, or package dependency was introduced.

The reviewed NumericBox addition exports only `MyDmsVn.Bootstrap5WinFormUI.Controls.BootstrapNumericBox : System.Windows.Forms.UserControl`. Its declared surface is the parameterless constructor; `Value`, `Minimum`, `Maximum`, `Increment`, `DecimalPlaces`, `ThousandsSeparator`, `ReadOnly`, `ValidationState`, and `BorderRadius`; the `ValueChanged` event; and only the protected lifecycle/interaction overrides required by the native-backed wrapper (`Dispose`, `OnDpiChangedAfterParent`, `OnEnabledChanged`, `OnEnter`, `OnFontChanged`, `OnLayout`, `OnMouseDown`, and `OnPaint`). `BootstrapNumericBoxRenderLogic` plus its palette/metrics/layout implementation types remain internal/private. The owned native WinForms `NumericUpDown` remains private and no public/protected child accessor was introduced. `BootstrapValidationState` remains exactly `None = 0`, `Valid = 1`, and `Invalid = 2`. No Hexadecimal/Accelerations/custom-parser/adornment API, timer, animation scheduler, popup/window host, new theme service, geometry type, or package dependency was introduced.

The reviewed ComboBox addition exports only `MyDmsVn.Bootstrap5WinFormUI.Controls.BootstrapComboBox : System.Windows.Forms.ComboBox`. Its declared public surface is the parameterless constructor plus `ValidationState`, `BorderRadius`, `LeadingIcon`, and `IconRenderer`. The only declared protected surface added by the native-backed implementation is `Dispose(bool)`, `OnDrawItem(DrawItemEventArgs)`, `OnHandleCreated(EventArgs)`, and `WndProc(ref Message)`, required for deterministic theme/font cleanup, owner-draw presentation, handle restoration, and conservative post-native shell painting. All `Items`, `DataSource`, `DisplayMember`, `ValueMember`, `SelectedIndex`, `SelectedItem`, `SelectedValue`, autocomplete properties, keyboard/focus behavior, dropdown lifecycle, and native events remain inherited WinForms API rather than being re-declared or mirrored. ComboBox render/palette/metrics/layout helpers remain internal/private. No item wrapper, custom popup/list host, `BootstrapDropdown` dependency, timer, animation scheduler, native-private-field reflection, P/Invoke hook, new theme service, geometry type, or package dependency was introduced.

The reviewed Dropdown addition exports `MyDmsVn.Bootstrap5WinFormUI.Controls.BootstrapDropdownItemKind` with exactly `Item = 0` and `Separator = 1`; `BootstrapDropdownItem` with its parameterless and kind constructors, immutable `Kind`, `Text`, `Icon`, `Enabled`, `Checked`, `Tag`, and `Click`; `BootstrapDropdownItemCollection : Collection<BootstrapDropdownItem>` with the public parameterless constructor inherited from its declared type plus protected `InsertItem` / `SetItem` null-guard overrides; and `BootstrapDropdown : Component` with its parameterless constructor, `Target`, `Items`, `Variant`, `MinimumWidth`, `Opened`, `Closed`, `Show()`, `Close()`, and protected `Dispose(bool)`. `BootstrapDropdownRenderer`, palette/metric helpers, the owned `ToolStripDropDownMenu`, all transient native `ToolStripMenuItem` / `ToolStripSeparator` instances, generated icon bitmaps, and target/theme wiring remain internal/private. No public submenu/split-button API, custom popup/window host, global hook, animation/timer surface, live collection-binding engine, or package dependency was introduced.

The reviewed Toast addition exports `MyDmsVn.Bootstrap5WinFormUI.Controls.BootstrapToastPlacement` with exactly `TopLeft = 0`, `TopRight = 1`, `BottomLeft = 2`, and `BottomRight = 3`; `BootstrapToast : UserControl` with its parameterless constructor, `Title`, `Variant`, `Icon`, `IconRenderer`, `Dismissible`, `AutoHide`, `AutoHideDelay`, `AnimationDuration`, `Dismissed`, `Dismiss()`, `GetPreferredSize(Size)`, and the protected lifecycle/painting overrides required by the custom surface (`Dispose`, `OnDpiChangedAfterParent`, `OnEnabledChanged`, `OnFontChanged`, `OnLayout`, `OnPaint`, and `OnTextChanged`); and `BootstrapToastContainer : Panel` with its parameterless constructor, `Placement`, `ToastSpacing`, `MaximumVisibleToasts`, `ShowToast(BootstrapToast)`, `DismissAll()`, plus protected `Dispose(bool)`, `OnDpiChangedAfterParent(EventArgs)`, and `OnSizeChanged(EventArgs)`. Feedback palette/layout, Toast layout/metrics, ownership host-state, auto-hide timer abstraction/implementation, animation factory seams, queue entries, and test doubles remain internal/private. No global/static Toast service, top-level notification window, public scheduler/timer seam, second feedback palette, or package dependency was introduced.

The reviewed DatePicker addition exports only `MyDmsVn.Bootstrap5WinFormUI.Controls.BootstrapDatePicker : System.Windows.Forms.UserControl`. Its declared public surface is the parameterless constructor; `Value`, `MinDate`, `MaxDate`, `Format`, `CustomFormat`, `ShowCheckBox`, `Checked`, `ValidationState`, and `BorderRadius`; and the `ValueChanged` event. Its declared protected surface is limited to the wrapper lifecycle/interaction overrides required by the native-backed composition: `Dispose(bool)`, `OnDpiChangedAfterParent(EventArgs)`, `OnEnabledChanged(EventArgs)`, `OnEnter(EventArgs)`, `OnFontChanged(EventArgs)`, `OnLayout(LayoutEventArgs)`, `OnMouseDown(MouseEventArgs)`, and `OnPaint(PaintEventArgs)`. `BootstrapDatePickerRenderLogic` and its palette/metrics helpers remain internal/private, and the sole owned native `DateTimePicker` remains private with `ShowUpDown = false`. No nullable-date API, custom parser/formatter/culture abstraction, custom calendar or popup host, `BootstrapDropdown` dependency, timer/animation scheduler, P/Invoke/private-WinForms reflection, public child accessor, new enum, or package dependency was introduced.

The Stage 3 fingerprint was approved only after CI printed the reconstructed exported surface and the Tooltip section was checked against the plan. That reviewed fingerprint was `b5eba7ddc68201d597cb1cdb7494ee1e7a259431f57e228e3711d0f8dfcd0b78`.

The Stage 4 fingerprint was likewise approved only after CI first failed the compatibility gate and printed the reconstructed exported surface. Review confirmed that the only intended additions were `BootstrapTabStyle` and the native-backed `BootstrapTabControl` surface described above. That reviewed fingerprint was `6fa7cadf03af069a07dd9a5eff38ed53a8b248664aba4f03343fcbeb2fc209a9`.

The Stage 5 fingerprint was approved only after CI on the completed Tasks 1–6 HEAD first failed the compatibility gate and printed the reconstructed exported surface. Review confirmed that the only intended addition was the native-backed `BootstrapNumericBox` surface described above, helper types stayed internal, the native editor stayed private, no existing exported type/member changed signature, and the compatibility assembly version stayed `1.0.0.0`. The reviewed fingerprint is `7e8be98bb686b9971a9f2da1c8eb9438416167c3894987cdc1d266910a2cce76`.

The Stage 6 fingerprint was approved only after CI first failed the compatibility gate and printed the reconstructed exported surface. Review confirmed that the only intended addition was the native-backed `BootstrapComboBox` surface described above, the four framework properties were the only new public members, the four protected overrides were implementation-required native extensibility points, helper types stayed internal, native binding/selection/autocomplete/dropdown surface remained inherited, and `AssemblyVersion` stayed `1.0.0.0`. The reviewed fingerprint was `3725b5012381672dcdcb64e8151ea4a4ec6c463e4ccf3ab65dedce278bb74a21`.

The Stage 7 fingerprint was approved only after CI first built both target frameworks with zero warnings/errors and then failed the compatibility test using the previous Stage 6 hash. The emitted reflection surface was reviewed before this document and the test constant were changed: the only additions were the Dropdown enum/model/collection/component surface described above; renderer, native menu, generated image resources and wiring remained implementation details; no existing exported member changed; and `AssemblyVersion` remained `1.0.0.0`. The reviewed Stage 7 fingerprint is `3dc6e527ca14c24aa2742b390cf7d0d345eb6c71657299c24c6a2459a88045c7`.

The Stage 8 fingerprint was approved only after repeated CI runs built both target frameworks with zero warnings/errors and the compatibility gate deliberately failed against the Stage 7 hash while printing the reconstructed exported surface. Review confirmed that the only intended additions were `BootstrapToastPlacement`, `BootstrapToast`, and `BootstrapToastContainer` with the declared surface described above; feedback palette/layout, timer, ownership, transition, and test seams remained internal/private; no existing exported signature changed; and `AssemblyVersion` remained `1.0.0.0`. The reviewed Stage 8 fingerprint is `31481edfafc1dbe40f802cb1535c31ab759b674b4b2cad84e5907b90d77173f0`.

The Stage 9 fingerprint was approved only after the public-API gate deliberately failed against the Stage 8 hash and the reconstructed export was reviewed. The only intended addition is the native-backed `BootstrapDatePicker` surface described above; the native `DateTimePicker`, render/layout helpers, validation/theme/DPI implementation details, and calendar popup remain private or internal; no existing exported signature changed; and `AssemblyVersion` remains `1.0.0.0`. The reviewed Stage 9 fingerprint is `b8c1cde896505856249b97c0b90f254861782d1835be3abb8aed305811202e24`.

`Phase16PublicApiBaselineTests.ExportedApiMatchesApprovedV1Baseline` deterministically reconstructs the surface with reflection and fails when the fingerprint changes. Its failure output contains the reconstructed surface so reviewers can inspect the change before approving a new fingerprint.

## Version compatibility policy

- `1.0.0-rc.*`: release-candidate validation of the proposed v1 surface. Any API change requires explicit review, documentation, and an intentional baseline update.
- `1.0.0` and later `1.x`: breaking changes to the frozen public/protected surface are not allowed. Compatible additions use a minor version; compatible fixes use a patch version.
- A deliberate breaking change after stable v1 requires the next major version.

Assembly compatibility remains `AssemblyVersion` `1.0.0.0` for the v1 line. Package versions use Semantic Versioning independently so RC/minor/patch releases do not force assembly-binding churn.

## What counts as a baseline change

Changes include removing or renaming a type/member, changing visibility, signature, base type, enum values, constructors, or protected extensibility hooks. Additions also change the fingerprint and therefore require review even when they are source/binary compatible.

The fingerprint gate is a review trigger, not a substitute for engineering judgment. A changed fingerprint must never be accepted merely to make CI green.
